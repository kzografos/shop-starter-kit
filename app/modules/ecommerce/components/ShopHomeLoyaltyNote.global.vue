<template>
  <span class="flex items-center gap-2">
    <UIcon name="i-heroicons-star" class="w-5 h-5 text-gold" />
    {{ $t('loyalty.earn_info') }}
  </span>
</template>

<script setup lang="ts">
// The loyalty promise in the home page's closing banner, contributed through
// app.config `homeBannerItems` (E8d4). The wording is the shop's, so it lives
// with the module that runs the programme rather than in the project's shell.
</script>
